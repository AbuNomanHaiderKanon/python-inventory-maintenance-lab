[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "inventory-maintenance-lab"
version = "0.1.0"
description = "A maintainable SQLite inventory and order management CLI"
requires-python = ">=3.10"
dependencies = []
